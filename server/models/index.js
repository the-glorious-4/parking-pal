const User = require('./User');
const ParkingPlace = require('./ParkingPlace');
const Inventory = require('./Inventory');
const Reservation = require('./Reservation');

module.exports = { User, ParkingPlace, Inventory, Reservation };
